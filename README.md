# POST-request
Technical sample for Apprenticeship application for Lyft.

A small web application in Python which accept a POST request to the route “/test”, which accepts one argument “string_to_cut” and return a JSON object with the key “return_string” and a string containing every third letter from the original string.
